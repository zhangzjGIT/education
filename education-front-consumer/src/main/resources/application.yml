eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/
server:
  port: 8092
spring:
  application:
    name: front-comsumer
    thymeleaf:
      cache: false
      prefix: classpath:/templates/
      suffix: .html

#feign:
#  hystrix:
#    enabled: true

#  httpclient:
#    enabled: true

